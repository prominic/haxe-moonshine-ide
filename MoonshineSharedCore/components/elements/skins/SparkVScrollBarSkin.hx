/**
 *  Generated by mxmlc 4.0
 *
 *  Package:    elements.skins
 *  Class:      SparkVScrollBarSkin
 *  Source:     /Users/axgord/dev/Moonshine-IDE/ide/MoonshineSharedCore/src/elements/skins/SparkVScrollBarSkin.mxml
 *  Template:   flex2/compiler/mxml/gen/ClassDef.vm
 *  Time:       2019.04.22 23:25:49 MSK
 */

package elements.skins;

import elements.skins.SparkVScrollBarThumbSkin;
import elements.skins.SparkVScrollBarTrackSkin;
import flash.accessibility.*;
import flash.debugger.*;
import flash.display.*;
import flash.errors.*;
import flash.events.*;
import flash.external.*;
import flash.geom.*;
import flash.media.*;
import flash.net.*;
import flash.printing.*;
import flash.profiler.*;
import flash.system.*;
import flash.text.*;
import flash.ui.*;
import flash.utils.*;
import flash.xml.*;
import mx.binding.*;
import mx.core.ClassFactory;
import mx.core.DeferredInstanceFromClass;
import mx.core.DeferredInstanceFromFunction;
import mx.core.IDeferredInstance;
import mx.core.IFactory;
import mx.core.IFlexModuleFactory;
import mx.core.IPropertyChangeNotifier;
import mx.core.IStateClient2;
import mx.core.Mx_internal;
import mx.filters.*;
import mx.states.SetProperty;
import mx.states.State;
import mx.styles.*;
import spark.components.Button;
import spark.skins.SparkSkin;

@:meta(HostComponent(name = 'spark.components.HScrollBar'))
@:meta(States(name = 'normal', name = 'disabled', name = 'inactive'))
//  begin class def
class SparkVScrollBarSkin extends spark.skins.SparkSkin implements mx.core.IStateClient2 {

	//  instance variables
	@:meta(Bindable())
	/**
	 * @private
	 **/
	public var thumb:spark.components.Button;

	@:meta(Bindable())
	/**
	 * @private
	 **/
	public var track:spark.components.Button;

	//  type-import dummies

	//  constructor (Flex display object)
	/**
	 * @private
	 **/
	public function new() {
		super();

		// mx_internal::_document = this;

		// layer initializers

		// properties
		this.name = 'TrackThumbOnlyVSBSkin';
		this.minWidth = 15;
		this.minHeight = 15;
		this.width = 15;
		this.mxmlContent = [_SparkVScrollBarSkin_Button1_i(), _SparkVScrollBarSkin_Button2_i()];
		this.currentState = 'normal';

		// events

		states = [
				new State({
					'name': 'normal',
					'overrides': []
				}),
				new State({
					'name': 'disabled',
					'overrides': [
					new mx.states.SetProperty().initializeFromObject({
								'name': 'alpha',
								'value': 0.5
							})
			]
				}),
				new State({
					'name': 'inactive',
					'overrides': [
					new mx.states.SetProperty().initializeFromObject({
								'name': 'alpha',
								'value': 0
							}),
					new mx.states.SetProperty().initializeFromObject({
								'target': 'thumb',
								'name': 'visible',
								'value': false
							})
			]
				})
		];

	}

	/**
	 * @private
	 **/
	private var __moduleFactoryInitialized:Bool = false;

	/**
	 * @private
	 * Override the module factory so we can defer setting style declarations
	 * until a module factory is set. Without the correct module factory set
	 * the style declaration will end up in the wrong style manager.
	 **/
	override private function set_moduleFactory(factory:IFlexModuleFactory):IFlexModuleFactory {
		super.moduleFactory = factory;

		if (__moduleFactoryInitialized) {
			return factory;
		}

		__moduleFactoryInitialized = true;

		// our style settings

		return factory;
	}

	//  initialize()
	/**
	 * @private
	 **/
	override public function initialize():Void {
		super.initialize();
	}

	//  scripts
	//  <Script>, line 25 - 32

	private static var exclusions(default, never):Array<Dynamic> = cast ['track', 'thumb'];

	override private function get_colorizeExclusions():Array<Dynamic> {
		return exclusions;
	}

	//  end scripts

	//  supporting function definitions for properties, events, styles, effects
	private function _SparkVScrollBarSkin_Button1_i():spark.components.Button {
		var temp:spark.components.Button = new spark.components.Button();
		temp.top = 0;
		temp.bottom = 0;
		temp.width = 15;
		temp.focusEnabled = false;
		temp.setStyle('skinClass', elements.skins.SparkVScrollBarTrackSkin);
		temp.id = 'track';
		if (!AS3.as(temp.document, Bool)) {
			temp.document = this;
		}
		track = temp;
		mx.binding.BindingManager.executeBindings(this, 'track', track);
		return temp;
	}

	private function _SparkVScrollBarSkin_Button2_i():spark.components.Button {
		var temp:spark.components.Button = new spark.components.Button();
		temp.width = 15;
		temp.focusEnabled = false;
		temp.setStyle('skinClass', elements.skins.SparkVScrollBarThumbSkin);
		temp.id = 'thumb';
		if (!AS3.as(temp.document, Bool)) {
			temp.document = this;
		}
		thumb = temp;
		mx.binding.BindingManager.executeBindings(this, 'thumb', thumb);
		return temp;
	}

}

//  end package def