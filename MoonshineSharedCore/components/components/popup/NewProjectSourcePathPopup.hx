/**
 *  Generated by mxmlc 4.0
 *
 *  Package:    components.popup
 *  Class:      NewProjectSourcePathPopup
 *  Source:     /Users/axgord/dev/Moonshine-IDE/ide/MoonshineSharedCore/src/components/popup/NewProjectSourcePathPopup.mxml
 *  Template:   flex2/compiler/mxml/gen/ClassDef.vm
 *  Time:       2019.04.22 23:25:49 MSK
 */

package components.popup;

import mx.events.FlexEvent;
import mx.events.ListEvent;
import actionScripts.events.NewFileEvent;
import actionScripts.factory.FileLocation;
import actionScripts.locator.IDEModel;
import actionScripts.utils.UtilsCore;
import actionScripts.valueObjects.ConstantsCoreVO;
import actionScripts.valueObjects.FileWrapper;
import actionScripts.valueObjects.ProjectVO;

import actionScripts.plugin.findreplace.view.PromptTextInput;
import actionScripts.ui.renderers.FTETreeItemRenderer;
import actionScripts.ui.resizableTitleWindow.ResizableTitleWindow;
import actionScripts.utils.CustomTreeFolders;
import components.skins.ResizableTitleWindowSkin;
import flash.accessibility.*;
import flash.debugger.*;
import flash.display.*;
import flash.errors.*;
import flash.events.*;
import flash.events.MouseEvent;
import flash.external.*;
import flash.geom.*;
import flash.media.*;
import flash.net.*;
import flash.printing.*;
import flash.profiler.*;
import flash.system.*;
import flash.text.*;
import flash.ui.*;
import flash.utils.*;
import flash.xml.*;
import mx.binding.*;
import mx.binding.IBindingClient;
import mx.controls.Image;
import mx.controls.Spacer;
import mx.core.ClassFactory;
import mx.core.DeferredInstanceFromClass;
import mx.core.DeferredInstanceFromFunction;
import mx.core.IDeferredInstance;
import mx.core.IFactory;
import mx.core.IFlexModuleFactory;
import mx.core.IPropertyChangeNotifier;
import mx.core.Mx_internal;

import mx.filters.*;
import mx.styles.*;
import mx.validators.StringValidator;
import spark.components.Button;
import spark.components.HGroup;
import spark.components.Image;
import spark.components.Label;
import spark.components.RadioButtonGroup;
import spark.components.VGroup;

@:meta(Event(name = 'EVENT_NEW_FILE', type = 'actionScripts.events.NewFileEvent'))
//  begin class def
class NewProjectSourcePathPopup extends actionScripts.ui.resizableTitleWindow.ResizableTitleWindow implements mx.binding.IBindingClient {

	//  instance variables
	/**
	 * @private
	 **/
	public var _NewProjectSourcePathPopup_Image1:mx.controls.Image;

	@:meta(Bindable())
	/**
	 * @private
	 **/
	public var btnCreate:spark.components.Button;

	@:meta(Bindable())
	/**
	 * @private
	 **/
	public var gandu:spark.components.VGroup;

	@:meta(Bindable())
	/**
	 * @private
	 **/
	public var hgExistingWarning:spark.components.HGroup;

	@:meta(Bindable())
	/**
	 * @private
	 **/
	public var rgModifiersA:spark.components.RadioButtonGroup;

	@:meta(Bindable())
	/**
	 * @private
	 **/
	public var sValidator:mx.validators.StringValidator;

	@:meta(Bindable())
	/**
	 * @private
	 **/
	public var tree:actionScripts.utils.CustomTreeFolders;

	@:meta(Bindable())
	/**
	 * @private
	 **/
	public var txtDestination:actionScripts.plugin.findreplace.view.PromptTextInput;

	//  type-import dummies

	//  constructor (Flex display object)
	/**
	 * @private
	 **/
	public function new() {
		super();

		// mx_internal::_document = this;

		var bindings:Array<Dynamic> = _NewProjectSourcePathPopup_bindingsSetup();
		var watchers:Array<Dynamic> = [];

		var target:Dynamic = this;

		if (_watcherSetupUtil == null) {
			var watcherSetupUtilClass:Dynamic = Type.resolveClass('_components_popup_NewProjectSourcePathPopupWatcherSetupUtil');
			Reflect.field(watcherSetupUtilClass, 'init')(null);
		}

		_watcherSetupUtil.setup(this,
				function(propertyName:String):Dynamic {
					return Reflect.field(target, propertyName);
				},
				function(propertyName:String):Dynamic {
					return Reflect.getProperty(NewProjectSourcePathPopup, propertyName);
				},
				bindings,
				watchers
		);

		// mx_internal::_bindings =  //  mx_internal::_bindings.concat(bindings);
		// mx_internal::_watchers =  //  mx_internal::_watchers.concat(watchers);

		// layer initializers

		// properties
		this.width = 500;
		this.minWidth = 400;
		this.height = 400;
		this.minHeight = 400;
		this.controlBarContent = [_NewProjectSourcePathPopup_Image1_i(), _NewProjectSourcePathPopup_Spacer1_c(), _NewProjectSourcePathPopup_Button1_i()];
		this.mxmlContentFactory = new mx.core.DeferredInstanceFromFunction(_NewProjectSourcePathPopup_Array2_c);
		_NewProjectSourcePathPopup_RadioButtonGroup1_i();
		_NewProjectSourcePathPopup_StringValidator1_i();

		// events
		this.addEventListener('creationComplete', ___NewProjectSourcePathPopup_ResizableTitleWindow1_creationComplete);

		for (i in 0...bindings.length) {
			AS3.as(bindings[i], Binding).execute();
		}

	}

	/**
	 * @private
	 **/
	private var __moduleFactoryInitialized:Bool = false;

	/**
	 * @private
	 * Override the module factory so we can defer setting style declarations
	 * until a module factory is set. Without the correct module factory set
	 * the style declaration will end up in the wrong style manager.
	 **/
	override private function set_moduleFactory(factory:IFlexModuleFactory):IFlexModuleFactory {
		super.moduleFactory = factory;

		if (__moduleFactoryInitialized) {
			return factory;
		}

		__moduleFactoryInitialized = true;

		// our style settings
		//  initialize component styles
		if (!AS3.as(this.styleDeclaration, Bool)) {
			this.styleDeclaration = new CSSStyleDeclaration(null, styleManager);
		}

		this.styleDeclaration.defaultFactory = function():Void {
					this.backgroundColor = 16119285;
					this.skinClass = components.skins.ResizableTitleWindowSkin;
				};
		return factory;
	}

	//  initialize()
	/**
	 * @private
	 **/
	override public function initialize():Void {
		super.initialize();
	}

	//  scripts
	//  <Script>, line 35 - 103

	@:meta(Bindable())public var wrapperBelongToProject:ProjectVO;
	@:meta(Bindable())public var isInterfaceDialog:Bool = false;

	@:meta(Bindable())public var wrapperOfFolderLocation:FileWrapper;
	public var folderLocation:FileLocation;

	@:meta(Bindable())private var model:IDEModel = IDEModel.getInstance();

	private function onCreateButton(event:MouseEvent):Void {
		// double check when double-clicked by tree
		if (!AS3.as(btnCreate.enabled, Bool)) {
			return;
		}

		var tmpEvent:NewFileEvent = new NewFileEvent(NewFileEvent.EVENT_FILE_SELECTED, null, null, wrapperOfFolderLocation);
		dispatchEvent(tmpEvent);

		super.closeThis();
	}

	private function onCreationCompletes(event:FlexEvent):Void {
		minHeight = height;
		title = 'Main source folder';
		txtDestination.text = Std.string(wrapperBelongToProject.folderLocation.fileBridge.name);
		btnCreate.enabled = false;

		var timeoutValue:Int = as3hx.Compat.setTimeout(function():Void {
					wrapperOfFolderLocation = wrapperBelongToProject.projectFolder;

					// since we're not updating children list at the time of
					// project creation dialog - we need to do that now do
					// display children list
					wrapperOfFolderLocation.isRoot = wrapperOfFolderLocation.shallUpdateChildren = true;
					wrapperOfFolderLocation.updateChildren();

					tree.dataProvider = wrapperBelongToProject.projectFolder;
					tree.callLater(parseFolders);
					as3hx.Compat.clearTimeout(timeoutValue);
				}, 1000);
	}

	private function parseFolders():Void {
		tree.expandItem(wrapperOfFolderLocation, true);
		tree.selectedItem = wrapperOfFolderLocation;
	}

	private function onTreeItemClicked(event:ListEvent):Void {
		txtDestination.text = UtilsCore.getPackageReferenceByProjectPath(
						[wrapperBelongToProject.folderLocation],
						Std.string(tree.selectedItem.nativePath)
			);
		wrapperOfFolderLocation = AS3.as(tree.selectedItem, FileWrapper);
	}

	//  end scripts

	//  supporting function definitions for properties, events, styles, effects
	private function _NewProjectSourcePathPopup_RadioButtonGroup1_i():spark.components.RadioButtonGroup {
		var temp:spark.components.RadioButtonGroup = new spark.components.RadioButtonGroup();
		temp.initialized(this, 'rgModifiersA');
		rgModifiersA = temp;
		mx.binding.BindingManager.executeBindings(this, 'rgModifiersA', rgModifiersA);
		return temp;
	}

	private function _NewProjectSourcePathPopup_StringValidator1_i():mx.validators.StringValidator {
		var temp:mx.validators.StringValidator = new mx.validators.StringValidator();
		temp.property = 'text';
		temp.initialized(this, 'sValidator');
		sValidator = temp;
		mx.binding.BindingManager.executeBindings(this, 'sValidator', sValidator);
		return temp;
	}

	private function _NewProjectSourcePathPopup_Image1_i():mx.controls.Image {
		var temp:mx.controls.Image = new mx.controls.Image();
		temp.width = 10;
		temp.height = 10;
		temp.id = '_NewProjectSourcePathPopup_Image1';
		if (!AS3.as(temp.document, Bool)) {
			temp.document = this;
		}
		_NewProjectSourcePathPopup_Image1 = temp;
		mx.binding.BindingManager.executeBindings(this, '_NewProjectSourcePathPopup_Image1', _NewProjectSourcePathPopup_Image1);
		return temp;
	}

	private function _NewProjectSourcePathPopup_Spacer1_c():mx.controls.Spacer {
		var temp:mx.controls.Spacer = new mx.controls.Spacer();
		temp.percentWidth = 100.0;
		if (!AS3.as(temp.document, Bool)) {
			temp.document = this;
		}
		return temp;
	}

	private function _NewProjectSourcePathPopup_Button1_i():spark.components.Button {
		var temp:spark.components.Button = new spark.components.Button();
		temp.label = 'Select';
		temp.styleName = 'darkButton';
		temp.addEventListener('click', __btnCreate_click);
		temp.id = 'btnCreate';
		if (!AS3.as(temp.document, Bool)) {
			temp.document = this;
		}
		btnCreate = temp;
		mx.binding.BindingManager.executeBindings(this, 'btnCreate', btnCreate);
		return temp;
	}

	/**
	 * @private
	 **/
	public function __btnCreate_click(event:flash.events.MouseEvent):Void {
		onCreateButton(event);
	}

	private function _NewProjectSourcePathPopup_Array2_c():Array<Dynamic> {
		var temp:Array<VGroup> = [_NewProjectSourcePathPopup_VGroup1_c()];
		return cast temp;
	}

	private function _NewProjectSourcePathPopup_VGroup1_c():spark.components.VGroup {
		var temp:spark.components.VGroup = new spark.components.VGroup();
		temp.percentHeight = 100.0;
		temp.percentWidth = 100.0;
		temp.gap = 10;
		temp.paddingLeft = 12;
		temp.paddingBottom = 3;
		temp.paddingTop = 9;
		temp.paddingRight = 13;
		temp.horizontalAlign = 'center';
		temp.mxmlContent = [_NewProjectSourcePathPopup_VGroup2_i(), _NewProjectSourcePathPopup_HGroup1_i()];
		if (!AS3.as(temp.document, Bool)) {
			temp.document = this;
		}
		return temp;
	}

	private function _NewProjectSourcePathPopup_VGroup2_i():spark.components.VGroup {
		var temp:spark.components.VGroup = new spark.components.VGroup();
		temp.percentWidth = 100.0;
		temp.percentHeight = 100.0;
		temp.mxmlContent = [_NewProjectSourcePathPopup_Label1_c(), _NewProjectSourcePathPopup_PromptTextInput1_i(), _NewProjectSourcePathPopup_CustomTreeFolders1_i()];
		temp.id = 'gandu';
		if (!AS3.as(temp.document, Bool)) {
			temp.document = this;
		}
		gandu = temp;
		mx.binding.BindingManager.executeBindings(this, 'gandu', gandu);
		return temp;
	}

	private function _NewProjectSourcePathPopup_Label1_c():spark.components.Label {
		var temp:spark.components.Label = new spark.components.Label();
		temp.text = 'Source Folder:';
		if (!AS3.as(temp.document, Bool)) {
			temp.document = this;
		}
		return temp;
	}

	private function _NewProjectSourcePathPopup_PromptTextInput1_i():actionScripts.plugin.findreplace.view.PromptTextInput {
		var temp:actionScripts.plugin.findreplace.view.PromptTextInput = new actionScripts.plugin.findreplace.view.PromptTextInput();
		temp.prompt = 'Select Destination';
		temp.editable = false;
		temp.percentWidth = 100.0;
		temp.styleName = 'textInput';
		temp.id = 'txtDestination';
		if (!AS3.as(temp.document, Bool)) {
			temp.document = this;
		}
		txtDestination = temp;
		mx.binding.BindingManager.executeBindings(this, 'txtDestination', txtDestination);
		return temp;
	}

	private function _NewProjectSourcePathPopup_CustomTreeFolders1_i():actionScripts.utils.CustomTreeFolders {
		var temp:actionScripts.utils.CustomTreeFolders = new actionScripts.utils.CustomTreeFolders();
		temp.percentWidth = 100.0;
		temp.percentHeight = 100.0;
		temp.rowHeight = 18;
		temp.labelField = 'name';
		temp.itemRenderer = _NewProjectSourcePathPopup_ClassFactory1_c();
		temp.doubleClickEnabled = true;
		temp.setStyle('color', 15658734);
		temp.setStyle('contentBackgroundColor', 0);
		temp.setStyle('rollOverColor', 3750201);
		temp.setStyle('selectionColor', 12674488);
		temp.setStyle('alternatingItemColors', [4473924, 5065804]);
		temp.setStyle('verticalScrollBarStyleName', 'black');
		temp.setStyle('borderVisible', false);
		temp.setStyle('useRollOver', true);
		temp.addEventListener('itemClick', __tree_itemClick);
		temp.addEventListener('doubleClick', __tree_doubleClick);
		temp.id = 'tree';
		if (!AS3.as(temp.document, Bool)) {
			temp.document = this;
		}
		tree = temp;
		mx.binding.BindingManager.executeBindings(this, 'tree', tree);
		return temp;
	}

	private function _NewProjectSourcePathPopup_ClassFactory1_c():mx.core.ClassFactory {
		var temp:mx.core.ClassFactory = new mx.core.ClassFactory();
		temp.generator = actionScripts.ui.renderers.FTETreeItemRenderer;
		return temp;
	}

	/**
	 * @private
	 **/
	public function __tree_itemClick(event:mx.events.ListEvent):Void {
		onTreeItemClicked(event);
	}

	/**
	 * @private
	 **/
	public function __tree_doubleClick(event:flash.events.MouseEvent):Void {
		onCreateButton(null);
	}

	private function _NewProjectSourcePathPopup_HGroup1_i():spark.components.HGroup {
		var temp:spark.components.HGroup = new spark.components.HGroup();
		temp.percentWidth = 100.0;
		temp.verticalAlign = 'middle';
		temp.mxmlContent = [_NewProjectSourcePathPopup_Image2_c(), _NewProjectSourcePathPopup_Label2_c()];
		temp.id = 'hgExistingWarning';
		if (!AS3.as(temp.document, Bool)) {
			temp.document = this;
		}
		hgExistingWarning = temp;
		mx.binding.BindingManager.executeBindings(this, 'hgExistingWarning', hgExistingWarning);
		return temp;
	}

	private function _NewProjectSourcePathPopup_Image2_c():spark.components.Image {
		var temp:spark.components.Image = new spark.components.Image();
		temp.source = _embed_mxml__elements_images_exclamation_png_1805742415;
		if (!AS3.as(temp.document, Bool)) {
			temp.document = this;
		}
		return temp;
	}

	private function _NewProjectSourcePathPopup_Label2_c():spark.components.Label {
		var temp:spark.components.Label = new spark.components.Label();
		temp.text = 'Source folder must be a sub-folder in project path';
		temp.percentWidth = 100.0;
		if (!AS3.as(temp.document, Bool)) {
			temp.document = this;
		}
		return temp;
	}

	/**
	 * @private
	 **/
	public function ___NewProjectSourcePathPopup_ResizableTitleWindow1_creationComplete(event:mx.events.FlexEvent):Void {
		onCreationCompletes(event);
	}

	//  binding mgmt
	private function _NewProjectSourcePathPopup_bindingsSetup():Array<Dynamic> {
		var result:Array<Dynamic> = [];

		result[0] = new mx.binding.Binding(this,
				function():Dynamic {
					return (new ConstantsCoreVO.loaderIcon());
				},
				null,
				'_NewProjectSourcePathPopup_Image1.source');

		result[1] = new mx.binding.Binding(this,
				function():Bool {
					return (tree.selectedItem == null);
				},
				null,
				'_NewProjectSourcePathPopup_Image1.visible');

		result[2] = new mx.binding.Binding(this,
				function():Bool {
					return (tree.selectedItem == null);
				},
				null,
				'_NewProjectSourcePathPopup_Image1.includeInLayout');

		result[3] = new mx.binding.Binding(this,
				function():Bool {
					return (tree.selectedIndex != 0);
				},
				null,
				'btnCreate.enabled');

		return result;
	}

	/**
	 * @private
	 **/
	public static var watcherSetupUtil(never, set):IWatcherSetupUtil2;
	private static function set_watcherSetupUtil(watcherSetupUtil:IWatcherSetupUtil2):IWatcherSetupUtil2 {
		(NewProjectSourcePathPopup)._watcherSetupUtil = watcherSetupUtil;
		return watcherSetupUtil;
	}

	private static var _watcherSetupUtil:IWatcherSetupUtil2;

	//  embed carrier vars
	@:meta(Embed(source = '/elements/images/exclamation.png'))
	private var _embed_mxml__elements_images_exclamation_png_1805742415:Class<Dynamic>;

	//  end embed carrier vars

	//  binding management vars
	/**
	 * @private
	 **/
	@:ns('mx_internal') private var _bindings:Array<Dynamic> = [];
	/**
	 * @private
	 **/
	@:ns('mx_internal') private var _watchers:Array<Dynamic> = [];
	/**
	 * @private
	 **/
	@:ns('mx_internal') private var _bindingsByDestination:Dynamic = {};
	/**
	 * @private
	 **/
	@:ns('mx_internal') private var _bindingsBeginWithWord:Dynamic = {};

}

//  end package def