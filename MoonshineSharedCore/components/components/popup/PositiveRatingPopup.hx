/**
 *  Generated by mxmlc 4.0
 *
 *  Package:    components.popup
 *  Class:      PositiveRatingPopup
 *  Source:     /Users/axgord/dev/Moonshine-IDE/ide/MoonshineSharedCore/src/components/popup/PositiveRatingPopup.mxml
 *  Template:   flex2/compiler/mxml/gen/ClassDef.vm
 *  Time:       2019.04.22 23:25:47 MSK
 */

package components.popup;

import actionScripts.valueObjects.ConstantsCoreVO;

import actionScripts.ui.resizableTitleWindow.ResizableTitleWindow;
import components.skins.ResizableTitleWindowSkin;
import flash.accessibility.*;
import flash.debugger.*;
import flash.display.*;
import flash.errors.*;
import flash.events.*;
import flash.events.MouseEvent;
import flash.external.*;
import flash.geom.*;
import flash.media.*;
import flash.net.*;
import flash.printing.*;
import flash.profiler.*;
import flash.system.*;
import flash.text.*;
import flash.ui.*;
import flash.utils.*;
import flash.xml.*;
import mx.binding.*;
import mx.binding.IBindingClient;
import mx.controls.Spacer;
import mx.core.ClassFactory;
import mx.core.DeferredInstanceFromClass;
import mx.core.DeferredInstanceFromFunction;
import mx.core.IDeferredInstance;
import mx.core.IFactory;
import mx.core.IFlexModuleFactory;
import mx.core.IPropertyChangeNotifier;
import mx.core.Mx_internal;
import mx.filters.*;
import mx.styles.*;
import spark.components.Button;
import spark.components.Label;
import spark.components.TextArea;
import spark.components.VGroup;

//  begin class def
class PositiveRatingPopup extends actionScripts.ui.resizableTitleWindow.ResizableTitleWindow implements mx.binding.IBindingClient {

	//  instance variables
	@:meta(Bindable())
	/**
	 * @private
	 **/
	public var txtMessage:spark.components.TextArea;

	//  type-import dummies

	//  constructor (Flex display object)
	/**
	 * @private
	 **/
	public function new() {
		super();

		// mx_internal::_document = this;

		var bindings:Array<Dynamic> = _PositiveRatingPopup_bindingsSetup();
		var watchers:Array<Dynamic> = [];

		var target:Dynamic = this;

		if (_watcherSetupUtil == null) {
			var watcherSetupUtilClass:Dynamic = Type.resolveClass('_components_popup_PositiveRatingPopupWatcherSetupUtil');
			Reflect.field(watcherSetupUtilClass, 'init')(null);
		}

		_watcherSetupUtil.setup(this,
				function(propertyName:String):Dynamic {
					return Reflect.field(target, propertyName);
				},
				function(propertyName:String):Dynamic {
					return Reflect.getProperty(PositiveRatingPopup, propertyName);
				},
				bindings,
				watchers
		);

		// mx_internal::_bindings =  //  mx_internal::_bindings.concat(bindings);
		// mx_internal::_watchers =  //  mx_internal::_watchers.concat(watchers);

		// layer initializers

		// properties
		this.minWidth = 400;
		this.width = 500;
		this.minHeight = 200;
		this.height = 200;
		this.title = 'Thank you!';
		this.controlBarContent = [_PositiveRatingPopup_Button1_c(), _PositiveRatingPopup_Spacer1_c(), _PositiveRatingPopup_Button2_c()];
		this.mxmlContentFactory = new mx.core.DeferredInstanceFromFunction(_PositiveRatingPopup_Array2_c);

		// events

		for (i in 0...bindings.length) {
			AS3.as(bindings[i], Binding).execute();
		}

	}

	/**
	 * @private
	 **/
	private var __moduleFactoryInitialized:Bool = false;

	/**
	 * @private
	 * Override the module factory so we can defer setting style declarations
	 * until a module factory is set. Without the correct module factory set
	 * the style declaration will end up in the wrong style manager.
	 **/
	override private function set_moduleFactory(factory:IFlexModuleFactory):IFlexModuleFactory {
		super.moduleFactory = factory;

		if (__moduleFactoryInitialized) {
			return factory;
		}

		__moduleFactoryInitialized = true;

		// our style settings
		//  initialize component styles
		if (!AS3.as(this.styleDeclaration, Bool)) {
			this.styleDeclaration = new CSSStyleDeclaration(null, styleManager);
		}

		this.styleDeclaration.defaultFactory = function():Void {
					this.skinClass = components.skins.ResizableTitleWindowSkin;
				};
		return factory;
	}

	//  initialize()
	/**
	 * @private
	 **/
	override public function initialize():Void {
		super.initialize();
	}

	//  scripts
	//  <Script>, line 26 - 81

	private function onButtonShareOnTwitterClick(event:MouseEvent):Void {
		var shareOnTwitterRequest:URLRequest = new URLRequest('https://twitter.com/intent/tweet');
		var urlVariables:URLVariables = new URLVariables();
		urlVariables.text = txtMessage.text;

		shareOnTwitterRequest.data = urlVariables;
		flash.Lib.getURL(shareOnTwitterRequest, '_blank');

		closeThis();
	}

	private function onButtonShareOnFacebookClick(event:MouseEvent):Void {
		var shareOnFacebookRequest:URLRequest = new URLRequest('http://www.facebook.com/dialog/share');
		var urlVariables:URLVariables = new URLVariables();
		urlVariables.app_id = '249823608417885';
		urlVariables.href = 'https://moonshine-ide.com/';
		urlVariables.hashtag = getFirstHashTag();
		urlVariables.quote = getTextWithoutHashTags();
		urlVariables.redirect_uri = 'https://moonshine-ide.com/';

		shareOnFacebookRequest.data = urlVariables;
		flash.Lib.getURL(shareOnFacebookRequest, '_blank');

		closeThis();
	}

	private function getFirstHashTag():String {
		var message:String = Std.string(txtMessage.text);
		if (message != null) {
			var indexOfFirstHashTag:Int = message.indexOf('#');
			var indexOfSpaceAfterHashTag:Int = message.indexOf(' ', indexOfFirstHashTag);

			return message.substring(indexOfFirstHashTag, indexOfSpaceAfterHashTag);
		}

		return '';
	}

	private function getTextWithoutHashTags():String {
		var message:String = Std.string(txtMessage.text);
		if (message != null) {
			var indexOfFirstHashTag:Int = message.indexOf('#');
			return message.substring(0, indexOfFirstHashTag);
		}

		return '';
	}

	//  end scripts

	//  supporting function definitions for properties, events, styles, effects
	private function _PositiveRatingPopup_Button1_c():spark.components.Button {
		var temp:spark.components.Button = new spark.components.Button();
		temp.label = 'Share on Facebook';
		temp.styleName = 'darkButton';
		temp.addEventListener('click', ___PositiveRatingPopup_Button1_click);
		if (!AS3.as(temp.document, Bool)) {
			temp.document = this;
		}
		return temp;
	}

	/**
	 * @private
	 **/
	public function ___PositiveRatingPopup_Button1_click(event:flash.events.MouseEvent):Void {
		onButtonShareOnFacebookClick(event);
	}

	private function _PositiveRatingPopup_Spacer1_c():mx.controls.Spacer {
		var temp:mx.controls.Spacer = new mx.controls.Spacer();
		temp.percentWidth = 100.0;
		if (!AS3.as(temp.document, Bool)) {
			temp.document = this;
		}
		return temp;
	}

	private function _PositiveRatingPopup_Button2_c():spark.components.Button {
		var temp:spark.components.Button = new spark.components.Button();
		temp.label = 'Share on Twitter';
		temp.styleName = 'darkButton';
		temp.addEventListener('click', ___PositiveRatingPopup_Button2_click);
		if (!AS3.as(temp.document, Bool)) {
			temp.document = this;
		}
		return temp;
	}

	/**
	 * @private
	 **/
	public function ___PositiveRatingPopup_Button2_click(event:flash.events.MouseEvent):Void {
		onButtonShareOnTwitterClick(event);
	}

	private function _PositiveRatingPopup_Array2_c():Array<Dynamic> {
		var temp:Array<VGroup> = [_PositiveRatingPopup_VGroup1_c()];
		return cast temp;
	}

	private function _PositiveRatingPopup_VGroup1_c():spark.components.VGroup {
		var temp:spark.components.VGroup = new spark.components.VGroup();
		temp.percentWidth = 100.0;
		temp.percentHeight = 100.0;
		temp.paddingLeft = 12;
		temp.paddingBottom = 12;
		temp.paddingTop = 9;
		temp.paddingRight = 12;
		temp.mxmlContent = [_PositiveRatingPopup_Label1_c(), _PositiveRatingPopup_TextArea1_i()];
		if (!AS3.as(temp.document, Bool)) {
			temp.document = this;
		}
		return temp;
	}

	private function _PositiveRatingPopup_Label1_c():spark.components.Label {
		var temp:spark.components.Label = new spark.components.Label();
		temp.text = 'If you would like to help spread the word, you can share a message on Facebook or Twitter using the buttons below.';
		temp.percentWidth = 100.0;
		if (!AS3.as(temp.document, Bool)) {
			temp.document = this;
		}
		return temp;
	}

	private function _PositiveRatingPopup_TextArea1_i():spark.components.TextArea {
		var temp:spark.components.TextArea = new spark.components.TextArea();
		temp.percentWidth = 100.0;
		temp.percentHeight = 100.0;
		temp.prompt = 'Say something you liked about #moonshineide';
		temp.id = 'txtMessage';
		if (!AS3.as(temp.document, Bool)) {
			temp.document = this;
		}
		txtMessage = temp;
		mx.binding.BindingManager.executeBindings(this, 'txtMessage', txtMessage);
		return temp;
	}

	//  binding mgmt
	private function _PositiveRatingPopup_bindingsSetup():Array<Dynamic> {
		var result:Array<Dynamic> = [];

		result[0] = new mx.binding.Binding(this,
				function():String {
					var result:Dynamic = 'I had a nice experience using ' + (ConstantsCoreVO.MOONSHINE_IDE_LABEL) + '! #moonshineide #actionscript';
					return Std.string((result == null) ? null : Std.string(result));
				},
				null,
				'txtMessage.text');

		return result;
	}

	/**
	 * @private
	 **/
	public static var watcherSetupUtil(never, set):IWatcherSetupUtil2;
	private static function set_watcherSetupUtil(watcherSetupUtil:IWatcherSetupUtil2):IWatcherSetupUtil2 {
		(PositiveRatingPopup)._watcherSetupUtil = watcherSetupUtil;
		return watcherSetupUtil;
	}

	private static var _watcherSetupUtil:IWatcherSetupUtil2;

	//  embed carrier vars
	//  end embed carrier vars

	//  binding management vars
	/**
	 * @private
	 **/
	@:ns('mx_internal') private var _bindings:Array<Dynamic> = [];
	/**
	 * @private
	 **/
	@:ns('mx_internal') private var _watchers:Array<Dynamic> = [];
	/**
	 * @private
	 **/
	@:ns('mx_internal') private var _bindingsByDestination:Dynamic = {};
	/**
	 * @private
	 **/
	@:ns('mx_internal') private var _bindingsBeginWithWord:Dynamic = {};

}

//  end package def